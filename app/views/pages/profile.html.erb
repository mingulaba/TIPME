<h1>This is a dashboard of a team member</h1>

<h2>Hello,<br><%= @team_member.first_name %> 👋🏼</h2>

<div class="card-1 card-body py-5 px-md-5">
  <div class="row d-flex justify-content-center">
   <%= cl_image_tag(@team_member.photo.key, class: "avatar-large")%>
   
  </div>
</div>
<ul class="list-group">
  <% @team_member.tables.each do |table| %>
    <li class="list-group-item"><%= table.review %></li>
  <% end %>
  <p><%= current_user.email %></p>
   <p><%= @team_member.first_name %></p>
</ul>
<br>
